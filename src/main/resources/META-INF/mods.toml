modLoader="javafml"
loaderVersion="${loader_version_range}"
license="${mod_license}"
[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
authors="${mod_authors}"
mixins = [
    "apoex.mixins.json",
    "apoex.mekanism.mixins.json",
    "apoex.astralmekanism.mixins.json",
    "apoex.evolvedmekanism.mixins.json",
    #"apoex.evolvedmekanismextras.mixins.json",
    "apoex.mekanismextras.mixins.json",
    "apoex.mekanismgenerators.mixins.json"
]
description='''${mod_description}'''
# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.${mod_id}]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="${forge_version_range}" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the dependency is not mandatory
    # BEFORE - This mod is loaded BEFORE the dependency
    # AFTER - This mod is loaded AFTER the dependency
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT, or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.${mod_id}]]
    modId="minecraft"
    mandatory=true
    versionRange="${minecraft_version_range}"
    ordering="AFTER"
    side="BOTH"

[[dependencies.${mod_id}]]
modId="apotheosis"
mandatory=true
versionRange="${apo_version}"
ordering="NONE"
side="BOTH"

[[dependencies.${mod_id}]]
modId="apotheotic_additions"
mandatory=false
versionRange="${apo_additions_version}"
ordering="NONE"
side="BOTH"

[[dependencies.${mod_id}]]
modId = "apotheosis_modern_ragnarok"
mandatory = false
versionRange = "${apo_modern_ragnarok_version}"
ordering = "NONE"
side = "BOTH"

[[dependencies.${mod_id}]]
modId = "mekanism"
mandatory = false
versionRange = "${mekanism_version}"
ordering = "BEFORE"
side = "BOTH"

[[dependencies.${mod_id}]]
modId = "mekanismgenerators"
mandatory = false
versionRange = "${mekanism_version}"
ordering = "BEFORE"
side = "BOTH"

[[dependencies.${mod_id}]]
modId = "astral_mekanism"
mandatory = false
versionRange = "${astral_mekanism_version}"
ordering = "BEFORE"
side = "BOTH"

[[dependencies.${mod_id}]]
modId = "evolvedmekanism"
mandatory = false
versionRange = "${evolved_mekanism_version}"
ordering = "BEFORE"
side = "BOTH"

[[dependencies.${mod_id}]]
modId = "emextras"
mandatory = false
versionRange = "${evolved_mekanism_extras_version}"
ordering = "BEFORE"
side = "BOTH"

[[dependencies.${mod_id}]]
modId = "mekanism_extras"
mandatory = false
versionRange = "${mekanism_extras_version}"
ordering = "BEFORE"
side = "BOTH"

[[dependencies.${mod_id}]]
modId = "addonapi"
mandatory = true
versionRange = "${addonapi_version}"
ordering = "BEFORE"
side = "BOTH"

[[dependencies.${mod_id}]]
modId = "jei"
mandatory = false
versionRange = "${jei_version}"
ordering = "BEFORE"
side = "BOTH"

[[dependencies.${mod_id}]]
modId = "jade"
mandatory = false
versionRange = "${jade_version}"
ordering = "BEFORE"
side = "BOTH"